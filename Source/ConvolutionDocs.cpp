//
// Copyright (c) 2020-2020 Nicholas Corgan
// SPDX-License-Identifier: GPL-3.0-or-later
//
// This file was generated on 2020-06-26 17:04:44.152085.
//

/*
 * |PothosDoc GSM XCCH Encoder
 *
 * Takes in a bytestream of GSM XCCH data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_xcch_encoder()
 */
void gsm_xcch();

/*
 * |PothosDoc GPRS CS2 Encoder
 *
 * Takes in a bytestream of GPRS CS2 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GPRS
 * |keywords coder lte
 * |factory /fec/gprs_cs2_encoder()
 */
void gprs_cs2();

/*
 * |PothosDoc GPRS CS3 Encoder
 *
 * Takes in a bytestream of GPRS CS3 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GPRS
 * |keywords coder lte
 * |factory /fec/gprs_cs3_encoder()
 */
void gprs_cs3();

/*
 * |PothosDoc GSM RACH Encoder
 *
 * Takes in a bytestream of GSM RACH data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_rach_encoder()
 */
void gsm_rach();

/*
 * |PothosDoc GSM SCH Encoder
 *
 * Takes in a bytestream of GSM SCH data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_sch_encoder()
 */
void gsm_sch();

/*
 * |PothosDoc GSM TCH-FR Encoder
 *
 * Takes in a bytestream of GSM TCH-FR data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_fr_encoder()
 */
void gsm_tch_fr();

/*
 * |PothosDoc GSM TCH-HR Encoder
 *
 * Takes in a bytestream of GSM TCH-HR data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_hr_encoder()
 */
void gsm_tch_hr();

/*
 * |PothosDoc GSM TCH-AFS12.2 Encoder
 *
 * Takes in a bytestream of GSM TCH-AFS12.2 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs12_2_encoder()
 */
void gsm_tch_afs12_2();

/*
 * |PothosDoc GSM TCH-AFS10.2 Encoder
 *
 * Takes in a bytestream of GSM TCH-AFS10.2 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs10_2_encoder()
 */
void gsm_tch_afs10_2();

/*
 * |PothosDoc GSM TCH-AFS7.95 Encoder
 *
 * Takes in a bytestream of GSM TCH-AFS7.95 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs7_95_encoder()
 */
void gsm_tch_afs7_95();

/*
 * |PothosDoc GSM TCH-AFS7.4 Encoder
 *
 * Takes in a bytestream of GSM TCH-AFS7.4 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs7_4_encoder()
 */
void gsm_tch_afs7_4();

/*
 * |PothosDoc GSM TCH-AFS6.7 Encoder
 *
 * Takes in a bytestream of GSM TCH-AFS6.7 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs6_7_encoder()
 */
void gsm_tch_afs6_7();

/*
 * |PothosDoc GSM TCH-AFS5.9 Encoder
 *
 * Takes in a bytestream of GSM TCH-AFS5.9 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs5_9_encoder()
 */
void gsm_tch_afs5_9();

/*
 * |PothosDoc GSM TCH-AHS7.95 Encoder
 *
 * Takes in a bytestream of GSM TCH-AHS7.95 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs7_95_encoder()
 */
void gsm_tch_ahs7_95();

/*
 * |PothosDoc GSM TCH-AHS7.4 Encoder
 *
 * Takes in a bytestream of GSM TCH-AHS7.4 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs7_4_encoder()
 */
void gsm_tch_ahs7_4();

/*
 * |PothosDoc GSM TCH-AHS6.7 Encoder
 *
 * Takes in a bytestream of GSM TCH-AHS6.7 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs6_7_encoder()
 */
void gsm_tch_ahs6_7();

/*
 * |PothosDoc GSM TCH-AHS5.9 Encoder
 *
 * Takes in a bytestream of GSM TCH-AHS5.9 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs5_9_encoder()
 */
void gsm_tch_ahs5_9();

/*
 * |PothosDoc GSM TCH-AHS5.15 Encoder
 *
 * Takes in a bytestream of GSM TCH-AHS5.15 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs5_15_encoder()
 */
void gsm_tch_ahs5_15();

/*
 * |PothosDoc GSM TCH-AHS4.75 Encoder
 *
 * Takes in a bytestream of GSM TCH-AHS4.75 data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs4_75_encoder()
 */
void gsm_tch_ahs4_75();

/*
 * |PothosDoc WiMax FCH Encoder
 *
 * Takes in a bytestream of WiMax FCH data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/WiMax
 * |keywords coder lte
 * |factory /fec/wimax_fch_encoder()
 */
void wimax_fch();

/*
 * |PothosDoc LTE PBCH Encoder
 *
 * Takes in a bytestream of LTE PBCH data and outputs the bytestream, encoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/LTE
 * |keywords coder lte
 * |factory /fec/lte_pbch_encoder()
 */
void lte_pbch();


/*
 * |PothosDoc GSM XCCH Decoder
 *
 * Takes in a bytestream of encoded GSM XCCH data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_xcch_decoder()
 */
void gsm_xcch();

/*
 * |PothosDoc GPRS CS2 Decoder
 *
 * Takes in a bytestream of encoded GPRS CS2 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GPRS
 * |keywords coder lte
 * |factory /fec/gprs_cs2_decoder()
 */
void gprs_cs2();

/*
 * |PothosDoc GPRS CS3 Decoder
 *
 * Takes in a bytestream of encoded GPRS CS3 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GPRS
 * |keywords coder lte
 * |factory /fec/gprs_cs3_decoder()
 */
void gprs_cs3();

/*
 * |PothosDoc GSM RACH Decoder
 *
 * Takes in a bytestream of encoded GSM RACH data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_rach_decoder()
 */
void gsm_rach();

/*
 * |PothosDoc GSM SCH Decoder
 *
 * Takes in a bytestream of encoded GSM SCH data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_sch_decoder()
 */
void gsm_sch();

/*
 * |PothosDoc GSM TCH-FR Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-FR data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_fr_decoder()
 */
void gsm_tch_fr();

/*
 * |PothosDoc GSM TCH-HR Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-HR data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_hr_decoder()
 */
void gsm_tch_hr();

/*
 * |PothosDoc GSM TCH-AFS12.2 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AFS12.2 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs12_2_decoder()
 */
void gsm_tch_afs12_2();

/*
 * |PothosDoc GSM TCH-AFS10.2 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AFS10.2 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs10_2_decoder()
 */
void gsm_tch_afs10_2();

/*
 * |PothosDoc GSM TCH-AFS7.95 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AFS7.95 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs7_95_decoder()
 */
void gsm_tch_afs7_95();

/*
 * |PothosDoc GSM TCH-AFS7.4 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AFS7.4 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs7_4_decoder()
 */
void gsm_tch_afs7_4();

/*
 * |PothosDoc GSM TCH-AFS6.7 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AFS6.7 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs6_7_decoder()
 */
void gsm_tch_afs6_7();

/*
 * |PothosDoc GSM TCH-AFS5.9 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AFS5.9 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_afs5_9_decoder()
 */
void gsm_tch_afs5_9();

/*
 * |PothosDoc GSM TCH-AHS7.95 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AHS7.95 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs7_95_decoder()
 */
void gsm_tch_ahs7_95();

/*
 * |PothosDoc GSM TCH-AHS7.4 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AHS7.4 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs7_4_decoder()
 */
void gsm_tch_ahs7_4();

/*
 * |PothosDoc GSM TCH-AHS6.7 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AHS6.7 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs6_7_decoder()
 */
void gsm_tch_ahs6_7();

/*
 * |PothosDoc GSM TCH-AHS5.9 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AHS5.9 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs5_9_decoder()
 */
void gsm_tch_ahs5_9();

/*
 * |PothosDoc GSM TCH-AHS5.15 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AHS5.15 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs5_15_decoder()
 */
void gsm_tch_ahs5_15();

/*
 * |PothosDoc GSM TCH-AHS4.75 Decoder
 *
 * Takes in a bytestream of encoded GSM TCH-AHS4.75 data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/GSM
 * |keywords coder lte
 * |factory /fec/gsm_tch_ahs4_75_decoder()
 */
void gsm_tch_ahs4_75();

/*
 * |PothosDoc WiMax FCH Decoder
 *
 * Takes in a bytestream of encoded WiMax FCH data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/WiMax
 * |keywords coder lte
 * |factory /fec/wimax_fch_decoder()
 */
void wimax_fch();

/*
 * |PothosDoc LTE PBCH Decoder
 *
 * Takes in a bytestream of encoded LTE PBCH data and outputs the bytestream, decoded.
 * The convolution parameters used for this encoding are standard-specific
 * and are read-only.
 *
 * |category /FEC/LTE
 * |keywords coder lte
 * |factory /fec/lte_pbch_decoder()
 */
void lte_pbch();
